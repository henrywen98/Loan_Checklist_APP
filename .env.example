# Copy this file to `.env` and fill in your secrets.
# Never commit your actual `.env` with real keys.

############################
# Provider Selection (optional)
############################
# LLM provider to use by default: 'openai' or 'deepseek'
# If omitted, defaults to 'openai'
# LLM_PROVIDER=openai

############################
# OpenAI (optional if using DeepSeek only)
############################
# Your OpenAI API Key (used when provider is 'openai' or when you select OpenAI in the sidebar)
# OPENAI_API_KEY=sk-...
# Default model for OpenAI (optional). If omitted, defaults to 'gpt-5-mini'
# OPENAI_MODEL=gpt-5-mini

############################
# DeepSeek (recommended to keep key only in .env/secrets)
############################
# Your DeepSeek API Key (the app will NOT ask for it in the UI)
# DEEPSEEK_API_KEY=sk-...
# Default model for DeepSeek (optional). If omitted, defaults to 'deepseek-chat'
# DEEPSEEK_MODEL=deepseek-chat

# Copy this file to `.env` and fill in your secrets.
# Never commit your actual `.env` with real keys.

############################
# Provider Selection (optional)
############################
# LLM provider to use by default: 'openai' or 'deepseek'
# If omitted, defaults to 'openai'
LLM_PROVIDER=deepseek

############################
# Model Configuration
############################
# 全局模型设置 - 优先级最高，会覆盖下面的 provider-specific 设置
# LLM_MODEL=deepseek-reasoner

############################
# OpenAI Configuration
############################
# Your OpenAI API Key (used when provider is 'openai')
# OPENAI_API_KEY=sk-proj-...
# Default model for OpenAI (optional). If omitted, defaults to 'gpt-4o-mini'
# OPENAI_MODEL=gpt-4o-mini
# OpenAI API Base URL (optional, for custom endpoints)
# OPENAI_BASE_URL=

############################
# DeepSeek Configuration
############################
# Your DeepSeek API Key
# DEEPSEEK_API_KEY=sk-...
# Default model for DeepSeek (optional). If omitted, defaults to 'deepseek-chat'
# DEEPSEEK_MODEL=deepseek-chat
# DeepSeek API Base URL (optional). If omitted, defaults to 'https://api.deepseek.com'
# DEEPSEEK_BASE_URL=https://api.deepseek.com

############################
# Model Parameters
############################
# Temperature for chat completions (0.0 to 2.0)
# TEMPERATURE=0.2
